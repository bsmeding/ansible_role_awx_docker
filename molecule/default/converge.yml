---
- name: Converge AWX Docker
  hosts: all
  become: true
  vars:
    # Docker daemon (GitHub runner) bind sources must use this host path; it matches
    # the volume in molecule.yml. Keep custom Python under awx__host_root so it maps too.
    awx__docker_bind_prefix: "{{ (lookup('env', 'MOLECULE_PROJECT_DIRECTORY') | default(playbook_dir ~ '/../..', true)) }}/.molecule/awx-data"
    custom_python_dir: "{{ awx__host_root }}/python"
    awx__port_http: 80
    awx__port_https: 443
    awx__network: 'test_awx_network'
    awx__network_cidr: '172.16.24.0/24'
    awx__remove_existing_container: false
    awx__remove_existing_home_dir: false
    # Skip Docker service management in test environment
    docker_service_manage: false
  pre_tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 600
      when: ansible_facts['os_family'] == 'Debian'
      changed_when: false

    # degraded returns rc 1 (message often on stderr); do not match only stdout.
    - name: Wait for systemd to complete initialization
      ansible.builtin.command: systemctl is-system-running
      register: systemctl_status
      until: systemctl_status.rc | int in [0, 1]
      retries: 15
      delay: 3
      when: ansible_facts['service_mgr'] == 'systemd'
      changed_when: false
      # Do not set failed_when on rc>1: "starting" uses rc>1 and must be retried until running/degraded.

    - name: Check if Docker socket exists
      ansible.builtin.stat:
        path: /var/run/docker.sock
      register: docker_socket

    - name: Ensure Docker socket permissions are correct
      ansible.builtin.file:
        path: /var/run/docker.sock
        mode: '0666'
      when: docker_socket.stat.exists

  roles:
    # bsmeding.docker is omitted here: its apt repo tasks are not idempotent under Molecule
    # (second converge reports changed). Docker API access uses the mounted host socket;
    # python3-docker is installed in prepare.yml.
    - role: bsmeding.awx_docker
