DATABASES = {
    'default': {
        'ATOMIC_REQUESTS': True,
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': "{{ awx__db_name }}",
        'USER': "{{ awx__postgres_user }}",
        'PASSWORD': "{{ awx__postgres_password }}",
        'HOST': "{{ awx__name + '_db'  | default('postgres') }}",
        'PORT': "{{ awx__db_host_port | default('5432') }}",
    }
}

BROADCAST_WEBSOCKET_SECRET = "{{ awx__broadcast_websocket_secret | b64encode }}"