---
roles:
  - src: bsmeding.docker
collections:
  # Keep in sync with molecule/default/collections.yml (Molecule + CI use that file).
  - name: community.docker
    version: 4.8.7
  - name: ansible.posix
    version: 1.5.4
